#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * Return: 0 (Success)
 */
int main(void)
{
	for (a = 0; a <= 10; a++)
		putchar('a');
	putchar('\n');
	return (0);
}
