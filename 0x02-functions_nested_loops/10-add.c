#include "main.h"
/**
 * add - Adds two integers and returns the result
 * @m: first integer
 * @n: second integer
 * Return: addition of both
 */
int add(int m, int n)
{
	int sum = m + n;

	return (sum);
}
